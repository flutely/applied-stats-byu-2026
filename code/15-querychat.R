# 15-querychat.R
# Deck 04: Beyond prompts (querychat)
# Goal: add querychat into an existing Shiny app so the user can chat with a
# data frame.

library(shiny)
library(querychat)

# TODO: pick a dataset and wire querychat_init + querychat_server / querychat_ui into a Shiny app
